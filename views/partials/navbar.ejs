
<nav class="navbar sticky-top navbar-expand-md navbar-fixed-top" id="navbar">
    <div class="container-fluid">
      <a class="navbar-brand me-auto" href="/home" >artCollector</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <div class='me-auto'>
          <ul class="navbar-nav">
            <% if (currentUser) { %>
            <li class="nav-item">
              <a class="nav-link"  href="/collection">My Collection</a>
              <% } %>
            </li>
          </ul>
        </div>
      <div class="navbar-nav ms-auto">

        <% if (!currentUser) { %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/home">Sign in!</a>
            <% } else { %>
              <li class="nav-item">
                <a class="nav-link" href="/preferences">Preferences</a>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Log Out!</a>
            <% } %>
        </ul>
      </div>
    </div>
  </div>

  </nav>